package threads.synchroization;

public class RaceCondition {

    // what is a racecondition when 2 0r more threads operate on shared object in a non synchrozined way and give incorrect values
   // Race Condition: guard shared state with synchronized, use AtomicInteger,
    // or design with immutable/functional patterns so no two threads ever write the same variable.
}
