package networking.highlevelapis.websockets;

public class SampleWebSocketServer  {

    // A websocket creates a persistent connection between the
    // clinet and the server whihc can be used to send messages back and forth ,, goo
    // this is good for chat ,stock tocker colllaborative tools
    // it allows server to pushh message back to the clinet and vice versa
    // the websockets establich a conection usinh http handshake,low overhead and latency
    // communication using websockets involves more than tcp connection ,they use specific format ,they use binary framing format
    // and needs to be endcoded decoded before sending

    // java has a Wessocket class which is a cleint for server we need to download a ddependenyc in maven
}
