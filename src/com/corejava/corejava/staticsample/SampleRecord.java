package com.corejava.corejava.staticsample;

public class SampleRecord {

    // a record is smimilar to class minus all the boilerplate code
    // it cannot extend any class becoz it extends a record,but can implement
    // all the instance variables are final -- immutable class --> final means there cannot be any setters
    //we can create static / methods
    // when to create when the instance var do not change create a record
}
