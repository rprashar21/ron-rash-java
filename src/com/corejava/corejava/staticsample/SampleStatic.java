package com.corejava.corejava.staticsample;

public class SampleStatic {
    //static variable belongs to the class ie it is shared across all the instances of the class
    // used for storing counters generating unique Ids ,, storing some constatnt value;

    // static methods -- iniside this we cannot use this vkeyword ,, this talks about the instance of the current class

    // instance methods can access static methods and variables directly

    // when a method does not use any fields or instance methods make it static

    // What is a POJO ? class which has only one instance method ,, used to read/write data to databases file or streams
    // also called as an entity or DTO data transfer objects
}
