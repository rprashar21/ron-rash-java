package com.corejava.corejava.oops.inheritance;

public class SampleObjectClass {

  //  Inheritance allows one class (child) to reuse the fields and methods of another (parent). It’s an is-a relationship.
    // every class in java extends the object class which is in java.lang package

    // methods in object -- toString,equals,hascode,wait notify notifyAll, clone
}
